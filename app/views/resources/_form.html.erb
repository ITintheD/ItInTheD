<%= form_for(@resource) do |f| %>
  <% if @resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resource.errors.count, "error") %> prohibited this resource from being saved:</h2>

      <ul>
      <% @resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :resource_title %><br />
    <%= f.text_field :resource_title %>
  </div>
  <div class="field">
    <%= f.label :resource_url %><br />
    <%= f.text_field :resource_url %>
  </div>
  <div class="field">
    <%= f.label :resource_file %><br />
    <%= f.text_field :resource_file %>
  </div>
  <div class="field">
    <%= f.label :resource_count %><br />
    <%= f.number_field :resource_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
